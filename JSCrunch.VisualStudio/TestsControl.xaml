<UserControl x:Class="JSCrunch.VisualStudio.TestsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:visualStudio="clr-namespace:JSCrunch.VisualStudio"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">
    <Grid>
        <Button Margin="0,0" Height="24" Content="Add Item" Click="ButtonBase_OnClick" VerticalAlignment="Top" />
        <TreeView ItemsSource="{Binding TreeCollection}" Name="TestModelTreeView" Margin="0,24">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type visualStudio:Node}" ItemsSource="{Binding Path=Children}">
                    <TreeViewItem Header="{Binding Path=Name}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
